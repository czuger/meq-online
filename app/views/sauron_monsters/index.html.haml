.container

  %h3
    Monsters on board

  %hr

  - @monsters.each do |monster|
    .row.mt-2
      .col-3
        = @board.location_name(monster.location)
      .col-3
        = @board.monster_name(monster.code)
      .col-3
        = link_to 'Move', edit_sauron_sauron_monster_path(@actor, monster.id), role: :button, class: 'btn btn-secondary btn-block'

  .row
    .col-3.mt-2
      = link_to 'Back to actions', edit_sauron_sauron_actions_path(@actor), role: :button, class: 'btn btn-primary btn-block'

