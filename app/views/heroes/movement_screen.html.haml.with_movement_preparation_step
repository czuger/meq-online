.container


  %h3
    Movement screen

  .alert.alert-secondary
    %ul.mt-3
      %li At this step, you can decide to move one step further.

  = render partial: 'hero_info'

  %hr

  = form_tag hero_move_path(@actor) do
    .form-group
      .row

        .col-8
          - if @next_movement
            = hidden_field_tag :movement_id, @next_movement.id
            = button_tag "Move from #{@from} to #{@to}", role: :button, class: 'btn btn-primary btn-block'
          - else
            = button_tag 'No more movements scheduled', role: :button, class: 'btn btn-secondary btn-block disabled', disabled: true

        .col-3
          = link_to hero_movement_finished_path(@actor), role: :button, class: 'btn btn-danger btn-block' do
            Next to exploration step