%h1 Listing logs

%table.table
  %thead
    %tr
      %th Action
      %th Card used

  %tbody
    - @logs.each do |log|
      %tr
        %td= print_log(log)
        %td
          -if log.action == 'move'
            = card_used(log)

