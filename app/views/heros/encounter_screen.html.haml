.container
  %p#notice= notice

  %h3
    Encounter screen

  .alert.alert-secondary
    %ul.mt-3
      %li At this step, you can make an encounter.
      %li This step is not implemented now.

  = render partial: 'hero_info'

  %hr

  = form_tag hero_encounter_path(@actor) do
    .form-group
      .row

        .col-3
          = button_tag 'Do encounter', role: :button, class: 'btn btn-secondary btn-block'

        .col-3
          = link_to hero_encounter_finished_path(@actor), role: :button, class: 'btn btn-danger btn-block' do
            Finish turn