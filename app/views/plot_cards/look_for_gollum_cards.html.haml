.container

  %h3 Choose a Gollum card

  .alert.alert-secondary
    %ul.mt-3
      %li You have to choose a Gollum card

  - @gollum_cards_found.each do |card|
    = card_picture( card, selectable: :unique )

  = form_tag get_gollum_card_sauron_plot_cards_path(@actor), class: 'ml-3' do
    .row

      .col-2
      .col-2
        = hidden_field_tag :selected_card, nil
        = button_tag 'Keep card', role: :button, class: 'btn btn-secondary btn-block', id: :validate_unique, value: :keep, disabled: true
